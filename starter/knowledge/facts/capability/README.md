# Capability Scope

Capability-scope shared memory entries apply to a single cross-cutting capability. Name the subdirectory after the capability.

These entries are loaded via **B2 skill-body**: individual skills declare them under `## Pre-execution context`.

## Naming Convention

Use the capability name as the directory name (e.g., `capability/agent-orchestration/`).
